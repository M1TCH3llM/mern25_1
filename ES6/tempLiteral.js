// Template Literals - are ES6 feature that allows for multi-line strings and string interpolation.
// They are enclosed by backticks (``) instead of single or double quotes, allowing for
// easier string manipulation and embedding expressions within strings using `${expression}` syntax.

import { animalSounds, animalSoundsShorthand } from './shortHand.js';

